from setuptools import setup, find_packages

setup(
    name="literature_game",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        'kivy',
        # other dependencies...
    ],
) 